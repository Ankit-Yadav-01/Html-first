name: Message on pull request
on:
  pull_request:
    types:
      - opened

jobs:
  myjob1:
    runs-on: windows-latest
    
    steps:
      - name: for creator
        run:
          echo "Thankyou for your pull request we will get back to you"
          echo "I am still working on my workflow"
          
        
